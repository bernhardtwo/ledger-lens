---
description: Draft a technical spec for a feature before implementing it.
argument-hint: <feature name or short description>
---

Use the `spec-writer` subagent to draft a spec for: **$ARGUMENTS**

Then save the result to `docs/specs/` as `NNNN-kebab-case-name.md`, using the
next available number (check the folder first). Show me the saved path. Do not
start implementing until I approve the spec.
