# @ledger-lens/mcp-server

Domain MCP server (Phase 3). Will expose tools such as `query_transactions`,
`compute_metrics`, `detect_anomalies`, `reconcile_period` for the agent to call.

> Placeholder in Phase 0. Implemented with the MCP TypeScript SDK in Phase 3.
